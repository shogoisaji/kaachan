import React from 'react'
import { View, Button, FlatList, Text } from 'react-native'

export const DataListCard = (saveData) => {
    return (
        <View>
            <Text>saveData.title</Text>
            <Text>saveData.time</Text>
            <Text>saveData.createdAt</Text>
        </View>
    )
}
